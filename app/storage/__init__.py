"""Persistence layer.

Currently just durable notes (memory.py) — short facts the model should
always have available across conversations. Full conversation history/RAG
are still separate, undecided future work.
"""
