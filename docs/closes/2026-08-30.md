# Close — 2026-08-30 (desarrollo Kore)

Fase: **cerrar el piloto** (D31). Tono del usuario (D30) + gate de registro.

## A) Hecho hoy

- **D30 Tono del usuario** (`d86a2dd`, `c0e9308`): chips (trato/largo/calidez/humor/firma); onboarding + Más; `/tono`; Gmail reply usa el perfil, no “como Jon”. Web chat activa skills igual que Telegram.
- **D31 arrancado.** Piloto = 8 tareas técnicas. Stripe desde el principio (no Bizum). Wedge: Día + Gmail reply + una misión. Telegram sigue Jon-only.
- **Allowlist + landing** (`8f885d6`, en Fly): `PILOT_ALLOWLIST` vacío = nadie nuevo. Landing: *Pide acceso* (no registro abierto). Invitados: *Tengo invitación*. Login de cuentas existentes no cambia.
- Lista de invites: aún vacía (no hay emails que meter).

## B) Dónde lo dejamos

- Prod: https://kore.fly.dev/ — gate vivo. Sin `PILOT_ALLOWLIST` en Fly → nadie puede registrarse.
- Local sucio (sin commit): `Makefile` + `.env.example` + spike — targets `make allowlist` / `allowlist-add` / `allowlist-show` / `allowlist-clear`.
- Siguiente click al reabrir: **flag en cuenta** (`allowed` / `paid_until`) — TODO ítem 3. No hay invitados todavía; el make de la lista espera emails.

## C) Para seguir

1. Commit del Makefile (`make allowlist…`) si quieres usarlo al primer invite.
2. **Flag en cuenta** — cortar o dejar entrar sin tocar solo el env.
3. Luego: gasto por home → tope LLM → chip en Más → Stripe → página legal.
4. No: misión→tareas, layout 1.6, Telegram multi.

## D) Cierre

Gate cerrado. No hay invitados aún. Al volver: commit del make + flag en cuenta (o el primer email en la allowlist).
